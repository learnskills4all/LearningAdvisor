// Imports
import { QueryClient } from "react-query";

// Declare the client object for queries
const client = new QueryClient();

// Export the client object to reduce code duplication
export default client;
